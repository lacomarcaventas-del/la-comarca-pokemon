"use client";
import {useEffect,useState} from "react";
import {useRouter} from "next/navigation";
import {supabaseBrowser} from "../../../lib/supabase";
const labels:any={pending:"Pendiente",contacted:"Contactado",paid:"Pagado",preparing:"Preparando",shipped:"Enviado",completed:"Completado",cancelled:"Cancelado"};
export default function Operaciones(){const router=useRouter(),sb=supabaseBrowser();const[orders,setOrders]=useState<any[]>([]),[alerts,setAlerts]=useState<any[]>([]),[low,setLow]=useState<any[]>([]),[imports,setImports]=useState<any[]>([]),[checking,setChecking]=useState(true);
 useEffect(()=>{(async()=>{const{data:u}=await sb.auth.getUser();if(!u.user){router.replace('/login');return}const{data:p}=await sb.from('profiles').select('role').eq('id',u.user.id).maybeSingle();if(p?.role!=='admin'){router.replace('/login');return}const[o,a,c,i]=await Promise.all([sb.from('orders').select('id,customer_name,status,total,created_at').order('created_at',{ascending:false}).limit(100),sb.from('order_alerts').select('*').eq('status','open').order('created_at',{ascending:false}),sb.from('cards').select('id,name,stock,price,categories(name)').lte('stock',2).order('stock'),sb.from('inventory_import_audit').select('*').eq('status','pending').order('created_at',{ascending:false})]);setOrders(o.data||[]);setAlerts(a.data||[]);setLow(c.data||[]);setImports(i.data||[]);setChecking(false)})()},[]);
 const count=(s:string)=>orders.filter(o=>o.status===s).length;return checking?<main className="wrap"><div className="empty">Verificando acceso...</div></main>:<><header className="top"><a href="/admin">← Administración</a><b>Centro de Operaciones</b><a href="/operativo">Panel Operativo</a></header><main className="wrap"><div className="panel"><h1>Centro de Operaciones</h1><div className="row">{['pending','paid','preparing','shipped'].map(s=><div className="panel" key={s}><b>{labels[s]}</b><div style={{fontSize:28}}>{count(s)}</div></div>)}</div></div><div className="panel"><h2>🚨 Alertas pendientes ({alerts.length})</h2>{alerts.length?alerts.map(a=><div className="notice" key={a.id}>🔴 Pedido {a.order_id.slice(0,8).toUpperCase()} · {a.body}</div>):<p className="muted">Sin alertas pendientes.</p>}</div><div className="panel"><h2>📥 Importaciones pendientes ({imports.length})</h2>{imports.length?imports.map(i=><div className="notice" key={i.id}>{i.file_name} · {i.created_rows} productos · {i.details?.actor_email||i.actor_id}</div>):<p className="muted">No hay cargas pendientes.</p>}</div><div className="panel"><h2>⚠️ Stock bajo ({low.length})</h2>{low.length?low.slice(0,30).map(c=><div key={c.id} className="row"><b>{c.name}</b><span>Stock: {c.stock} · ${Number(c.price).toLocaleString('es-MX')}</span></div>):<p className="muted">Sin productos con stock bajo.</p>}</div><div className="panel"><h2>📦 Últimos pedidos</h2>{orders.slice(0,20).map(o=><div key={o.id} className="row"><span><b>#{o.id.slice(0,8).toUpperCase()}</b> · {o.customer_name||'Cliente'}</span><span>{labels[o.status]||o.status} · ${Number(o.total).toLocaleString('es-MX')}</span></div>)}</div></main></>}
