"use client";
import{useEffect,useState}from"react";import{useRouter}from"next/navigation";import{supabaseBrowser}from"../../../lib/supabase";
const labels:any={pending:"Pendiente",contacted:"Contactado",paid:"Pagado",preparing:"Preparando",shipped:"Enviado",completed:"Completado",cancelled:"Cancelado"};
const historyLabels:any={created:"Pedido creado",status_changed:"Estado cambiado",tracking_changed:"Seguimiento actualizado",updated:"Pedido actualizado"};
export default function Orders(){const[orders,setOrders]=useState<any[]>([]),[histories,setHistories]=useState<Record<string,any[]>>({}),[loading,setLoading]=useState(true),[msg,setMsg]=useState(""),[checking,setChecking]=useState(true),[editing,setEditing]=useState<string|null>(null),[carrier,setCarrier]=useState(""),[tracking,setTracking]=useState(""),router=useRouter(),sb=supabaseBrowser();
 useEffect(()=>{(async()=>{const{data}=await sb.auth.getUser();if(!data.user){router.replace('/login');return}const{data:profile}=await sb.from('profiles').select('role').eq('id',data.user.id).maybeSingle();if(profile?.role!=='admin'){await sb.auth.signOut();router.replace('/login');return}setChecking(false);load()})()},[]);
 async function load(){setLoading(true);const{data,error}=await sb.from('orders').select('*,order_items(quantity,unit_price,cards(name,card_number))').order('created_at',{ascending:false});if(error){setMsg(error.message);setLoading(false);return}setOrders(data||[]);const ids=(data||[]).map((o:any)=>o.id);if(ids.length){const{data:h}=await sb.from('order_history').select('*').in('order_id',ids).order('created_at',{ascending:false});const grouped:any={};(h||[]).forEach((x:any)=>(grouped[x.order_id]??=[]).push(x));setHistories(grouped)}else setHistories({});setLoading(false)}
 async function status(o:any,status:string){setMsg("");if(status==='shipped'&&(!o.tracking_number||!o.tracking_carrier)){setEditing(o.id);setCarrier(o.tracking_carrier||"");setTracking(o.tracking_number||"");setMsg('Para marcar como Enviado debes capturar paquetería y número de tracking.');return}const{error}=await sb.from('orders').update({status,updated_at:new Date().toISOString(),...(status==='shipped'?{shipped_at:o.shipped_at||new Date().toISOString()}:{})}).eq('id',o.id);if(error)setMsg(error.message);else load()}
 async function saveShipping(o:any){if(!carrier.trim()||!tracking.trim()){setMsg('Captura paquetería y número de tracking.');return}setMsg('');const{error}=await sb.from('orders').update({status:'shipped',tracking_carrier:carrier.trim(),tracking_number:tracking.trim(),shipped_at:o.shipped_at||new Date().toISOString(),updated_at:new Date().toISOString()}).eq('id',o.id);if(error){setMsg(error.message);return}setEditing(null);setCarrier("");setTracking("");setMsg('Pedido marcado como enviado y seguimiento guardado.');load()}
 async function cancelOrder(id:string){if(!confirm('¿Cancelar este pedido y devolver las cartas al inventario?'))return;setMsg("");const{data,error}=await sb.rpc('cancel_order',{p_order_id:id});if(error){setMsg(error.message);return}if(!data?.ok){setMsg('No se pudo cancelar el pedido.');return}setMsg('Pedido cancelado. El stock fue restaurado.');load()}
 async function deleteOrder(id:string){if(!confirm('¿ELIMINAR DEFINITIVAMENTE este pedido? Esta acción no se puede deshacer.'))return;setMsg("");const{data,error}=await sb.rpc('delete_order',{p_order_id:id});if(error){setMsg(error.message);return}if(!data?.ok){setMsg('No se pudo eliminar el pedido.');return}setMsg('Pedido eliminado y registrado en auditoría.');load()}
 if(checking)return <main className="wrap"><div className="empty">Verificando acceso...</div></main>;
 return <><header className="top"><a href="/admin">← Administración</a><b>Pedidos</b><a href="/operativo">Panel Operativo</a><a href="/catalogo">Catálogo</a></header><main className="wrap"><div className="panel"><h2>Pedidos</h2><button className="btn2" onClick={load}>Actualizar</button>{msg&&<p className="notice">{msg}</p>}</div>{loading?<div className="empty">Cargando...</div>:orders.length===0?<div className="empty">No hay pedidos.</div>:orders.map(o=><div className="panel" key={o.id}><div className="row"><div><b>Folio: {o.id.slice(0,8).toUpperCase()}</b><div className="muted">{new Date(o.created_at).toLocaleString('es-MX')}</div></div><div><b>{o.customer_name}</b><div className="muted">{o.customer_phone} · {o.customer_email||'sin correo'}</div></div><div><b>${Number(o.total).toLocaleString('es-MX')} MXN</b><div className="muted">{labels[o.status]||o.status}</div></div></div><hr/><ul>{(o.order_items||[]).map((i:any)=><li key={i.card?.name+i.quantity}>{i.quantity} × {i.card?.name||'Carta'} — ${Number(i.unit_price).toLocaleString('es-MX')}</li>)}</ul>{o.notes&&<div className="notice">Notas: {o.notes}</div>}
 {o.tracking_number&&<div className="notice" style={{marginTop:12}}><b>📦 Envío:</b> {o.tracking_carrier||'Paquetería'} · <b>{o.tracking_number}</b>{o.shipped_at&&<span className="muted"> · enviado {new Date(o.shipped_at).toLocaleString('es-MX')}</span>}</div>}
 {editing===o.id&&<div className="panel" style={{marginTop:12}}><h3>Datos de envío</h3><label>Paquetería</label><input value={carrier} onChange={e=>setCarrier(e.target.value)} placeholder="FedEx, DHL, Estafeta, etc."/><label>Número de tracking</label><input value={tracking} onChange={e=>setTracking(e.target.value)} placeholder="Número de guía"/><div className="actions" style={{marginTop:10}}><button className="btn" onClick={()=>saveShipping(o)}>Guardar y marcar enviado</button><button className="btn2" onClick={()=>setEditing(null)}>Cancelar</button></div></div>}
 <div className="actions" style={{marginTop:12}}>{['pending','contacted','paid','preparing','shipped','completed'].map(s=><button key={s} className={o.status===s?'btn':'btn2'} onClick={()=>status(o,s)}>{labels[s]}</button>)}<button className="btn2" onClick={()=>{setEditing(o.id);setCarrier(o.tracking_carrier||"");setTracking(o.tracking_number||"")}}>📦 Tracking</button>{o.status!=='cancelled'&&o.status!=='completed'&&<button className="danger" onClick={()=>cancelOrder(o.id)}>❌ Cancelar y devolver stock</button>}{o.status==='cancelled'&&<button className="danger" onClick={()=>deleteOrder(o.id)}>🗑️ Eliminar pedido</button>}</div>
 <details style={{marginTop:16}}><summary><b>Historial de cambios ({(histories[o.id]||[]).length})</b></summary><div style={{marginTop:10}}>{(histories[o.id]||[]).map((h:any)=><div key={h.id} className="muted" style={{padding:'7px 0',borderBottom:'1px solid rgba(127,127,127,.15)'}}><b>{historyLabels[h.action]||h.action}</b> · {new Date(h.created_at).toLocaleString('es-MX')}{h.old_status!==h.new_status&&<span> · {labels[h.old_status]||h.old_status||'—'} → {labels[h.new_status]||h.new_status||'—'}</span>}{h.new_tracking_number&&<span> · {h.new_tracking_carrier||'Paquetería'}: {h.new_tracking_number}</span>}</div>)}</div></details>
 </div>)}</main></>}
