import { NextRequest, NextResponse } from 'next/server';
import { createClient } from '@supabase/supabase-js';

function config(){const url=process.env.NEXT_PUBLIC_SUPABASE_URL;const anon=process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY;const service=process.env.SUPABASE_SERVICE_ROLE_KEY;return {url,anon,service}}
async function adminClient(req:NextRequest){const {url,anon,service}=config();if(!url||!anon||!service)throw new Error('Configuración administrativa incompleta.');const auth=req.headers.get('authorization')||'';const token=auth.startsWith('Bearer ')?auth.slice(7):'';const userClient=createClient(url,anon,{global:{headers:{Authorization:`Bearer ${token}`}}});const {data:{user},error}=await userClient.auth.getUser();if(error||!user)throw new Error('No autorizado.');const {data:profile}=await userClient.from('profiles').select('role').eq('id',user.id).maybeSingle();if(profile?.role!=='admin')throw new Error('Solo un administrador puede gestionar pedidos.');return createClient(url,service,{auth:{autoRefreshToken:false,persistSession:false}})}
export async function GET(req:NextRequest){try{const admin=await adminClient(req);const {data,error}=await admin.from('orders').select('*,order_items(quantity,unit_price,cards(name,card_number))').order('created_at',{ascending:false});if(error)throw error;const ids=(data||[]).map((o:any)=>o.id);let history:any[]=[];if(ids.length){const r=await admin.from('order_history').select('*').in('order_id',ids).order('created_at',{ascending:false});if(r.error)throw r.error;history=r.data||[]}return NextResponse.json({orders:data||[],history});}catch(e:any){const message=e?.message||'No se pudieron cargar los pedidos.';return NextResponse.json({error:message},{status:message==='No autorizado.'?401:message.includes('Solo un administrador')?403:500});}}
export async function PATCH(req:NextRequest){try{const admin=await adminClient(req);const body=await req.json();const {orderId,status,tracking_carrier,tracking_number}=body;if(!orderId) return NextResponse.json({error:'Pedido inválido.'},{status:400});const patch:any={updated_at:new Date().toISOString()};if(status)patch.status=status;if(tracking_carrier!==undefined)patch.tracking_carrier=tracking_carrier;if(tracking_number!==undefined)patch.tracking_number=tracking_number;if(status==='shipped')patch.shipped_at=new Date().toISOString();const {data,error}=await admin.from('orders').update(patch).eq('id',orderId).select().single();if(error)throw error;return NextResponse.json({ok:true,order:data});}catch(e:any){return NextResponse.json({error:e?.message||'No se pudo actualizar el pedido.'},{status:500});}}
